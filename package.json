{
  "name": "bjd-wins",
  "version": "1.0.0",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/QCMaxcer/BJD-Streaks.git"
  },
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
